import estilos from'./footer.module.css';

const Footer = () => {
    return (
        <footer className={estilos.holaHolis}>
            <h5 className={estilos.footer}>⚛️Fictional e-commerce store created with ReactJS⚛️</h5>
        </footer>
    );
};

export default Footer;
